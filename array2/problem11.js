//Combining push() and pop()
//Create an array of 3 countries. Add one more country using push(), then remove the last country using pop().
// Print the updated array and the removed country.

let country=["india","austrilia","pakisthan"]
let p=country.push("newzland")
console.log(p)
let a=country.pop("newzland");
console.log(a);
console.log(country);